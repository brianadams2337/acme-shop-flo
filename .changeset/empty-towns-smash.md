---
'@scayle/storefront-application-nuxt': patch
---

**\[E2E\]** Updated end-to-end test that verifies user orders. Order number is now separated from the headline by using symbol `#` instead of `:`, so the end-to-end test in `/playwright/tests/e2e-Orders.spec.ts` has been updated to match the changes accordingly.
