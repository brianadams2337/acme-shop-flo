# SCAYLE Storefront Boilerplate - /app

The `app/` directory is a core part of the framework’s structure, intended for enhancing or overriding default Nuxt functionality. This directory is specifically designed to contain files that customize the application's behavior at a global level, such as custom plugins, the main application layout, and additional routing configurations.
