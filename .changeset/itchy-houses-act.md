---
'@scayle/storefront-boilerplate-nuxt': patch
---

End-to-End test Country detector shop switcher - added method `first()` to always return the first matching element in case of multiple shop choices.
