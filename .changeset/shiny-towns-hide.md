---
'@scayle/storefront-application-nuxt': minor
---

**\[Account Area\]** Improved the navigation flow from the order detail page. The "Back" button now provides a more seamless experience by returning the user to the `/orders` list page, unless they were just on that page, in which case it functions as a standard browser back action.
