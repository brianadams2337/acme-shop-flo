---
'@scayle/storefront-application-nuxt': minor
---

**\[Account Area\]** Enhanced the behavior of the order detail back button.
It now redirects users to the previous page if it was the `orders` page; otherwise,
it defaults to navigating back to the `orders` route for a seamless experience.
